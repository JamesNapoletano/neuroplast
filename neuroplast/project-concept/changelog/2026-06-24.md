# 2026-06-24
#changelog

## Summary

### Claude Code Plugin
- Created the Neuroplast Claude Code plugin at `src/adapter-assets/claude-code/plugin/` (canonical source) and `neuroplast/adapter-assets/claude-code/plugin/` (installed copy).
- Plugin contents: `plugin.json`, `neuroplast-orchestrator` agent, `neuroplast-planner` agent, `neuroplast-bootstrap` skill, `neuroplast-route-short-prompts` skill, `neuroplast-execute-act` skill, `README.md` (7 files total).
- Plugin packages the same agents and skills already used from `~/.claude/` in this environment so users can install via plugin rather than manual file copying.
- `src/adapter-assets/claude-code/plugin/` is picked up automatically by the dynamic `adapterAssetFiles` listing in `src/cli/constants.js` — no code change was required.
- Updated `ARCHITECTURE.md`: added 7 new sync mappings in the file installation table and a plugin note in the portability section.
- Updated two test assertions in `test/cli-reliability.test.js`: `52 → 59` baselines adopted, `53 → 60` unchanged (reflecting 7 new managed files).

### Claude Code Adapter Sync Extension
- Created `neuroplast/local-extensions/claude-code-adapter-sync/` with `README.md`, `act.md`, `CHANGELOG_INSTRUCTIONS.md`, and `think.md`.
- Extension enforces complete artifact sync for the plugin: src-only edits, `npx neuroplast sync` propagation, installed-copy reinstall tracking, cross-adapter OpenCode alignment, and plugin version consistency.
- Activated in `neuroplast/manifest.yaml` under `extensions.active_local`.

### Plugin Install Script
- Created `src/adapter-assets/claude-code/install-plugin.js` and installed mirror at `neuroplast/adapter-assets/claude-code/install-plugin.js`.
- Script permanently registers the plugin in `~/.claude/plugins/installed_plugins.json` and `~/.claude/settings.json` so users don't need `--plugin-dir` per session.
- Uses `__dirname` to resolve the plugin path relative to the script's own location — works correctly in any project where Neuroplast is installed.
- Idempotent: skips steps already done, reports clearly on each path.
- Updated plugin `README.md` in both src and installed copies with correct install instructions (`node neuroplast/adapter-assets/claude-code/install-plugin.js`).
- Updated `ARCHITECTURE.md` to add `install-plugin.js` sync mapping.
- Updated two test assertions: `59 → 60` baselines adopted, `60 → 61` unchanged.
- Manually registered plugin permanently in this environment via the script.

### Concept Consistency
- Updated `neuroplast/project-concept/npm-package.md` to note that the Claude Code plugin is shipped as part of `adapter-assets/`.

## Related Plans
- [[plans/claude-code-plugin.md]]

## Sync Impact
- no migration needed — new adapter asset files are picked up automatically by the dynamic file listing; no content-transforming migration was required.

## Verification
- `npm test` — 45/45 pass (after each file addition, test assertions updated accordingly).
- `npx neuroplast validate` — 101 checks, 0 errors.
- Dynamic listing confirms all adapter asset files including `install-plugin.js` appear under `claude-code/`.
- Install script verified idempotent across two consecutive runs.

## README Impact
- No README change needed — the plugin is a new distributable artifact but does not change any command, path, or supported environment status documented in the README.

## Navigation
- Previous: [[project-concept/changelog/2026-06-11.md]]
- Next: _TBD_
